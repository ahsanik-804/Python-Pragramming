def phonebook_manager():
    phonebook = {}
    while True:
        name=input("Enter a name: ").strip().title()
        if name == "0":
            break
        number=int(input("Enter a phone number: ").strip())
        phonebook[name] = number
    print("Phonebook:", phonebook)
phonebook_manager()
